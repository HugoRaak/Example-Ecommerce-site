<div class="card">
    <div class="card-body">
        <h4 class="card-title">Articles en vente : {{countArticle}}</h4>
        <a href="{{pathFor('admin.article.index')}}" class="btn btn-primary">Gérer les articles</a>
    </div>
</div>
<div class="card mt-3">
    <div class="card-body">
        <h4 class="card-title">Catégories actuel : {{countCategorie}}</h4>
        <a href="{{pathFor('admin.categorie.index')}}" class="btn btn-primary">Gérer les catégories</a>
    </div>
</div>