{% if flash('success') %}
    <div class="alert alert-success">
        {{flash('success')}}
    </div>
{% elseif flash('error') %}
    <div class="alert alert-danger">
        {{flash('error')}}
    </div>
{% endif %}